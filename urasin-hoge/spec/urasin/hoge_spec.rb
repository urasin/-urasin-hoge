require 'spec_helper'

describe Urasin::Hoge do
  it 'has a version number' do
    expect(Urasin::Hoge::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
